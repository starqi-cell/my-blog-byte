# 🚀 快速启动指南

## 步骤 1: 安装依赖

```bash
npm install
```

## 步骤 2: 配置环境变量

`.env` 文件已创建，默认配置如下：
- MySQL: localhost:3306 (用户名: root, 密码: 123456)
- Redis: localhost:6379
- 后端端口: 4000
- 前端端口: 3000

如需修改，请编辑 `.env` 文件。

## 步骤 3: 启动数据库（使用 Docker）

```bash
# 启动 MySQL 和 Redis
docker-compose up -d mysql redis

# 查看服务状态
docker-compose ps

# 查看日志
docker-compose logs -f mysql redis
```

**重要提示**: 首次启动 MySQL 可能需要 20-30 秒初始化，请等待完成后再执行下一步。

## 步骤 4: 初始化数据库

```bash
# 等待 MySQL 完全启动后执行
npm run db:init
```

这将创建所有必要的表和示例数据。

## 步骤 5: 启动开发服务器

```bash
# 同时启动前端和后端
npm run dev
```

或者分别启动：

```bash
# 终端 1: 启动后端
npm run dev:server

# 终端 2: 启动前端
npm run dev:client
```

## 步骤 6: 访问应用

- 🌐 前端应用: http://localhost:3000
- 🔌 后端 API: http://localhost:4000/api
- 📊 健康检查: http://localhost:4000/api/health

## 🎯 测试账户

由于默认管理员密码需要 bcrypt 加密，建议通过注册页面创建第一个账户。

或者，你可以通过以下方式创建管理员账户：

```bash
# 进入 MySQL 容器
docker exec -it blog-mysql mysql -uroot -p123456 blog_db

# 执行 SQL（将 your_password 替换为 bcrypt 加密后的密码）
UPDATE users SET role = 'admin' WHERE id = 1;
```

## 📝 常见命令

### 开发环境

```bash
npm run dev          # 启动开发服务器
npm run db:init      # 初始化数据库
```

### 生产环境

```bash
npm run build        # 构建项目
npm run preview      # 预览生产版本
```

### Docker 部署

```bash
npm run docker:build # 构建 Docker 镜像
npm run docker:up    # 启动所有服务
npm run docker:down  # 停止所有服务
```

### 数据库管理

```bash
# 进入 MySQL 容器
docker exec -it blog-mysql mysql -uroot -p123456 blog_db

# 备份数据库
docker exec blog-mysql mysqldump -uroot -p123456 blog_db > backup.sql

# 恢复数据库
docker exec -i blog-mysql mysql -uroot -p123456 blog_db < backup.sql
```

## 🔧 故障排查

### 端口被占用

如果 3000 或 4000 端口被占用，可以修改 `.env` 文件中的端口配置。

### MySQL 连接失败

1. 确认 MySQL 容器正在运行: `docker-compose ps`
2. 查看 MySQL 日志: `docker-compose logs mysql`
3. 等待 MySQL 完全启动（首次启动需要时间）

### Redis 连接失败

1. 确认 Redis 容器正在运行: `docker-compose ps`
2. 测试连接: `docker exec -it blog-redis redis-cli ping`

### SSR 渲染错误

开发环境下，SSR 功能在生产模式才会启用。开发时前端通过 Vite 提供服务。

要测试 SSR，需要先构建：

```bash
npm run build
npm run preview
```

## 🎨 功能演示

1. **注册账户**: 访问 http://localhost:3000/#/register
2. **登录**: 使用注册的账户登录
3. **创建文章**: 点击右上角头像 → "写文章"
4. **使用 AI**: 在编辑器中点击 "AI 生成内容" 按钮
5. **发表评论**: 在文章详情页底部发表评论
6. **切换主题**: 点击顶部导航栏的灯泡图标

## 📚 下一步

- 阅读 `README.md` 了解项目详情
- 查看 `SETUP.md` 了解完整文档
- 探索代码了解实现细节

祝你使用愉快！🎉
